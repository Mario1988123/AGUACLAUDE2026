import { ModulePlaceholder } from "@/shared/components/module-placeholder";

export default function ProductosPage() {
  return (
    <ModulePlaceholder
      title="Productos"
      description="Catálogo con categorías, atributos, precios (contado/renting/alquiler con mínimos por nivel), stock, recambios compatibles, imágenes y ficha técnica."
    />
  );
}
