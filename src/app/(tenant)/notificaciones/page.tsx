import { ModulePlaceholder } from "@/shared/components/module-placeholder";

export default function NotificacionesPage() {
  return (
    <ModulePlaceholder
      title="Notificaciones"
      description="Stock bajo, leads próximos a caducar, instalaciones del día siguiente, cargas pendientes, pagos pendientes, aprobaciones de precio. Realtime via Supabase channels."
    />
  );
}
