import { ModulePlaceholder } from "@/shared/components/module-placeholder";

export default function LeadsPage() {
  return (
    <ModulePlaceholder
      title="Leads"
      description="Contactos potenciales. CRUD, asignación, conversión a cliente, llamada, WhatsApp, propuesta o prueba gratuita. Estados con caducidad configurable."
    />
  );
}
